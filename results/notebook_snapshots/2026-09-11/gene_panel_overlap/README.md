# Gene-panel overlap snapshots

These four executed notebooks are byte-preserved September 11 uploads for
BARseq, Projection-TAGs, simulation, and SPIDER spatial. They use four
repetitions and pin the pre-consolidation overlap core `bd77d41`.

Use the output-cleared notebooks under `notebooks/gene_panel_overlap/` for new
runs; do not relabel these outputs as results from the consolidated core.
