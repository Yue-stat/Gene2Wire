# Target-block masking snapshots

These six executed notebooks are byte-preserved September 11 uploads for
BARseq A1, BARseq M1, MERGE-seq, Projection-TAGs, simulation, and SPIDER
spatial. They pin pre-consolidation core `040a303`; only machine-specific
`N_JOBS` values differ from their corresponding released entry points.

Use the output-cleared notebooks under `notebooks/target_block_masking/` for new
runs; do not relabel these outputs as results from the consolidated core.
